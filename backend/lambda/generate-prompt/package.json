{
    "name": "generate-prompt",
    "version": "1.0.0",
    "description": "Lambda function for AI prompt enhancement",
    "main": "index.js",
    "scripts": {
        "test": "jest",
        "package": "zip -r function.zip index.js package.json node_modules"
    },
    "dependencies": {
        "aws-sdk": "^2.1500.0"
    },
    "devDependencies": {
        "jest": "^29.7.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
